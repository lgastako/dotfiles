[user]
        name        = John Evans
        email       = lgastako@gmail.com
[alias]
        u           = push
        d           = pull
        a           = add
        co          = checkout
        ci          = commit
        st          = status
        b           = branch
        r           = remote
        reb         = rebase
        pl          = pull --no-ff
        pn          = pull --no-ff
        p           = push
        qc          = commit --no-verify
        qcommit     = commit --no-verify
        merge       = merge --no-ff
        m           = merge --no-ff
[color]
        status      = auto
        diff        = auto
        branch      = auto
        interactive = auto
[core]
        excludesfile = ~/dotfiles/git/gitignore
[push]
        default = matching
